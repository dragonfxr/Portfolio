# Personal Portfolio
This is Russell's personal portfolio
https://russell-feng.netlify.app/

1. frontend:
   `npm install`

   `npm start`

3. backend:
   `npm install -g @sanity/cli`

   `sanity install`

   `sanity start`
